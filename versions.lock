# The set an image was built from. Written by 'make lock'.
#
# Read from build/staged.lock, which tools/stage.sh writes as it
# stages each tarball — so these are the commits a build actually
# consumed, not wherever HEAD has since moved to. That distinction
# is why this file was previously wrong: it named an avatari two
# commits ahead of the one inside the image.
#
# It still records what was built, not that it works. That word is
# earned by booting it, and is written below by hand.
# Generated 2026-09-08T22:01:11Z
avatari    = { version = "0.7.1", commit = "3a0ac471be9b5cf754f6beab585d6b95cc5d35a1" }
reflexi    = { version = "0.9.0", commit = "1e1a7c389c8b3bd85143d46e7715c2be6ae81db4" }
cogiti     = { version = "0.39.0", commit = "6073801db5f7a946c65f19aaacabf023a42900b5" }
audi       = { version = "0.3.0", commit = "a2ed91b591303813560c70fe41186fc16c11bf91" }
inteliboy  = { version = "0.19.0-1-gb8e31ec", commit = "b8e31ecafecba7f1bf5984757ee55c93307c64e8" }

# Booted, 2026-09-08, qemu on a copy of build/image.img: InteliBoy 0.19.0.
#   the face waits for the ears now. cogiti holds the eyes shut until the
#            speech adapter sends `ready` (speech-protocol.md §3), which audi
#            emits when its capture device opens and its streaming recogniser
#            is loaded. Bounded at 25s so an adapter that never says it
#            cannot leave a device asleep.
#   NOT PROVEN HERE. qemu has no soundcard: audi opened a capture that then
#            failed on its first read, so `ready` arrived and the adapter
#            died in a restart loop. The event therefore means "the device
#            opened", not "audio works" — which is the most any adapter can
#            honestly claim at that moment, and is still seconds later than
#            the old signal, which was merely "the process was spawned".
#            Only the hardware can show the difference.
#   get_version answers from /etc/os-release instead of the model guessing.
# Seeded with all three credentials. THIS IMAGE IS A SECRET.
