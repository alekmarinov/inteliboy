# The set an image was built from. Written by 'make lock'.
#
# Read from build/staged.lock, which tools/stage.sh writes as it
# stages each tarball — so these are the commits a build actually
# consumed, not wherever HEAD has since moved to. That distinction
# is why this file was previously wrong: it named an avatari two
# commits ahead of the one inside the image.
#
# It still records what was built, not that it works. That word is
# earned by booting it, and is written below by hand.
# Generated 2026-09-08T20:11:46Z
avatari    = { version = "0.7.1", commit = "3a0ac471be9b5cf754f6beab585d6b95cc5d35a1" }
reflexi    = { version = "0.8.0", commit = "4c761d15b202f3bb76e924513f1395fbe2e06793" }
cogiti     = { version = "0.37.0", commit = "594d6622943f5862d0354f4abe005d0b729d291b" }
audi       = { version = "0.2.0-2-ga77f58e", commit = "a77f58e2dc45000b6ce96db3c4fc08d11864cf4d" }
inteliboy  = { version = "0.18.0", commit = "3ccaf2100f1a79449341855e90f61154a25d34af" }

# Booted, 2026-09-08, qemu on a copy of build/image.img: InteliBoy 0.18.0.
#   lpkg 11, with the transaction lock. Before it, two upgrades at once
#            interleaved over the same files and the loser rebuilt its owners
#            index from a half-written database — which matters now that the
#            appliance runs upgrades on its own.
#   the duty ran on its own about fifteen seconds after cogiti started:
#            synced, "244 packages in channel, current", found nothing
#            upgradable and said nothing. Silence with nothing to report is
#            the designed behaviour; this image is newer than the channel was.
#   audi     restarts: qemu has no soundcard, so capture and playback is
#            UNPROVEN here.
# Seeded with all three credentials. THIS IMAGE IS A SECRET.
# Not booted on the hardware, and the notice itself is unwitnessed: it needs
# a device that is behind the channel, which this one is not.
